const $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const models=[
 {name:'Nemotron 3.5 Lightning',id:'nvidia/nemotron-3.5-lightning:free',desc:'高速・軽量。普段の会話や素早い回答に向いています。'},
 {name:'Nemotron 3 Ultra',id:'nvidia/nemotron-3-ultra-550b-a55b:free',desc:'高性能推論。複雑な質問や難しいタスク向けです。'},
 {name:'Nemotron 3 Nano Omni',id:'nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free',desc:'マルチモーダル推論。画像などを扱う用途に向いています。'},
 {name:'Gemma 4 26B',id:'google/gemma-4-26b-a4b-it:free',desc:'バランス型。日常会話から一般的な質問まで幅広く対応します。'},
 {name:'Nemotron 3 Super',id:'nvidia/nemotron-3-super-120b-a12b:free',desc:'高性能汎用。複雑な指示や難しいタスク向けです。'}
];
let chats=[{id:1,title:'新しいチャット'},{id:2,title:'アイデアを整理する'},{id:3,title:'Webアプリの設計について'},{id:4,title:'今日のメモ'}],active=1,contextChat=null,attachments=[];
const esc=s=>String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
function toast(t){const x=$('#toast');x.textContent=t;x.classList.add('show');clearTimeout(window.__toast);window.__toast=setTimeout(()=>x.classList.remove('show'),1800)}
function renderChats(){const box=$('#chatList');box.innerHTML=chats.map(c=>`<div class="chat-item ${c.id===active?'active':''}"><button class="chat-main">${esc(c.title)}</button><button class="chat-more">•••</button></div>`).join('');$$('.chat-item').forEach((el,i)=>{el.querySelector('.chat-main').onclick=()=>selectChat(chats[i].id);el.querySelector('.chat-more').onclick=e=>{e.stopPropagation();openContext(e.currentTarget,chats[i].id)}})}
function selectChat(id){active=id;const c=chats.find(x=>x.id===id);$('#chatTitle').textContent=c.title;renderChats();if(innerWidth<=760)$('#sidebar').classList.remove('open')}
renderChats();
$('#newChat').onclick=()=>{const id=Date.now();chats.unshift({id,title:'新しいチャット'});selectChat(id);$('#welcome').hidden=false;$('#messages').hidden=true};
$('#homeBtn').onclick=()=>{$('#welcome').hidden=false;$('#messages').hidden=true};
$('#openSide').onclick=()=>$('#sidebar').classList.add('open');$('#closeSide').onclick=()=>$('#sidebar').classList.remove('open');
$('#main').onclick=e=>{if(innerWidth<=760&&!e.target.closest('#sidebar'))$('#sidebar').classList.remove('open')};
function openContext(btn,id){contextChat=id;const m=$('#context'),r=btn.getBoundingClientRect();m.style.left=Math.min(r.left,innerWidth-170)+'px';m.style.top=Math.min(r.bottom+4,innerHeight-100)+'px';m.classList.add('show')}
document.addEventListener('click',e=>{if(!e.target.closest('.chat-more')&&!e.target.closest('#context'))$('#context').classList.remove('show')});
$('#rename').onclick=()=>{const c=chats.find(x=>x.id===contextChat),n=prompt('チャット名',c.title);if(n?.trim()){c.title=n.trim();selectChat(c.id)}$('#context').classList.remove('show')};
$('#delete').onclick=()=>{const c=chats.find(x=>x.id===contextChat);$('#confirmTitle').textContent=`「${c.title}」を削除しますか？`;$('#confirmBackdrop').classList.add('show');$('#context').classList.remove('show')};
$('#cancelDelete').onclick=()=>$('#confirmBackdrop').classList.remove('show');$('#doDelete').onclick=()=>{chats=chats.filter(c=>c.id!==contextChat);if(!chats.length)chats=[{id:Date.now(),title:'新しいチャット'}];active=chats[0].id;selectChat(active);$('#confirmBackdrop').classList.remove('show');toast('チャットを削除しました')};
function renderModels(){const menu=$('#modelMenu');menu.innerHTML=models.map((m,i)=>`<button class="model-option ${i===0?'selected':''}" data-i="${i}"><span class="dot"></span><div><b>${esc(m.name)}</b><small>${esc(m.desc)}</small></div></button>`).join('');$$('.model-option').forEach(b=>b.onclick=()=>{const m=models[+b.dataset.i];$('#modelLabel').textContent=m.name;$$('.model-option').forEach(x=>x.classList.remove('selected'));b.classList.add('selected');menu.classList.remove('show')});$('#settingsModel').innerHTML=models.map(m=>`<option>${esc(m.name)}</option>`).join('')}
renderModels();
$('#modelBtn').onclick=e=>{e.stopPropagation();$('#modelMenu').classList.toggle('show')};
$('#attach').onclick=e=>{e.stopPropagation();$('#fileMenu').classList.add('show');$('#fileOverlay').classList.add('show')};
function closeFile(){ $('#fileMenu').classList.remove('show');$('#fileOverlay').classList.remove('show') }
$('#fileOverlay').onclick=closeFile;$('#pickFiles').onclick=()=>{$('#fileInput').click();closeFile()};$('#cameraFiles').onclick=()=>{$('#fileInput').click();closeFile()};$('#demoFile').onclick=()=>{addDemoFiles();closeFile()};
$('#fileInput').onchange=e=>{[...e.target.files].forEach(addFile);e.target.value=''};
function addDemoFiles(){const data=[new File(['<h1>LUNA Preview</h1>'],'luna-preview.html',{type:'text/html'}),new File(['name,status\nLUNA,ready'],'result.csv',{type:'text/csv'}),new File(['{\n  "status": "ready"\n}'],'data.json',{type:'application/json'})];data.forEach(addFile);toast('デモファイルを追加しました')}
function addFile(file){attachments.push({id:Date.now()+Math.random(),file});renderAttachments()}
function renderAttachments(){const box=$('#attachments');box.innerHTML=attachments.map(a=>{const f=a.file,img=f.type.startsWith('image/');return `<div class="attachment"><span class="thumb">${img?`<img src="${URL.createObjectURL(f)}">`:iconFor(f)}</span><span class="file-meta"><b>${esc(f.name)}</b><small>${size(f.size)} · ${typeName(f)}</small></span><button class="remove-file" data-id="${a.id}">×</button></div>`}).join('');$$('.remove-file').forEach(b=>b.onclick=()=>{attachments=attachments.filter(a=>String(a.id)!==b.dataset.id);renderAttachments()})}
function iconFor(f){if(f.type==='application/pdf'||/\.pdf$/i.test(f.name))return'PDF';if(f.type==='text/html')return'HTML';if(f.type==='text/csv')return'CSV';if(f.type==='application/json')return'JSON';return'FILE'}function typeName(f){if(f.type.startsWith('image/'))return'画像';if(f.type==='application/pdf')return'PDF';if(f.type==='text/html')return'HTML';return'ドキュメント'}function size(n){return n<1024?n+' B':n<1048576?(n/1024).toFixed(1)+' KB':(n/1048576).toFixed(1)+' MB'}
['dragenter','dragover'].forEach(ev=>$('#composer').addEventListener(ev,e=>{e.preventDefault();$('#dropHint').classList.add('show')}));['dragleave','drop'].forEach(ev=>$('#composer').addEventListener(ev,e=>{e.preventDefault();$('#dropHint').classList.remove('show')}));$('#composer').addEventListener('drop',e=>[...e.dataTransfer.files].forEach(addFile));
const promptBox=$('#prompt');promptBox.addEventListener('input',()=>{promptBox.style.height='auto';promptBox.style.height=Math.min(promptBox.scrollHeight,180)+'px'});promptBox.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();send()}});$$('.suggestions button').forEach(b=>b.onclick=()=>{promptBox.value=b.textContent;promptBox.focus()});$('#send').onclick=send;
function send(){const text=promptBox.value.trim();if(!text&&!attachments.length)return;$('#welcome').hidden=true;$('#messages').hidden=false;const list=$('#messages');const user=document.createElement('article');user.className='message user';user.innerHTML=`<div class="bubble">${esc(text||'添付ファイルを送信しました。')}</div>${attachments.length?`<div class="sent-files">${attachments.map(a=>`<span>${esc(a.file.name)}</span>`).join('')}</div>`:''}<div class="actions"><button>✎ 編集</button><button data-copy>□ コピー</button></div>`;list.appendChild(user);const ai=document.createElement('article');ai.className='message ai';ai.innerHTML=demoAnswer();list.appendChild(ai);promptBox.value='';promptBox.style.height='auto';attachments=[];renderAttachments();bindMessageActions(ai);list.parentElement.scrollTop=list.parentElement.scrollHeight}
function demoAnswer(){return `<div class="answer"><div class="answer-head"><span class="ai-mark">L</span><b>LUNA</b><span class="badge">回答</span></div><h2>UIプレビューのサンプル</h2><p>これはLUNAの完成UIを確認するためのサンプル回答です。<strong>Markdown</strong>、リンク、引用、表、コード、ファイル出力などを一度に確認できます。</p><blockquote>重要な情報は一次情報を確認してください。</blockquote><h3>チェックリスト</h3><ul><li>通常のMarkdown表示</li><li>コードブロックとコピー</li><li>表とリンク</li><li>生成ファイル</li></ul><div class="code-card"><div class="code-head"><span>javascript</span><div><button data-copy-code>コピー</button><button data-download-code>ダウンロード</button></div></div><pre><code>const luna = {\n  status: 'ready',\n  ui: 'complete'\n};</code></pre></div><div class="table-card"><table><thead><tr><th>項目</th><th>状態</th><th>説明</th></tr></thead><tbody><tr><td>UI</td><td><span class="ok">完成</span></td><td>レビュー可能</td></tr><tr><td>API</td><td><span class="pending">未接続</span></td><td>次工程</td></tr></tbody></table></div><div class="preview-card"><div><span class="preview-icon">HTML</span><div><b>luna-preview.html</b><small>HTMLファイル · 2.4 KB</small></div></div><div class="preview-actions"><button data-preview>プレビュー</button><button data-download>↓ ダウンロード</button></div></div><div class="source-box"><div><b>Sources</b><small>Web検索結果をここに表示します</small></div><button>3件を見る ›</button></div><div class="answer-actions"><button data-regenerate>↻ 再生成</button><button data-copy>□ コピー</button><button>Sources</button></div></div>`}
function bindMessageActions(root){$$('[data-copy],[data-copy-code]',root).forEach(b=>b.onclick=()=>{navigator.clipboard?.writeText('LUNA UI sample');toast('コピーしました')});$$('[data-download],[data-download-code]',root).forEach(b=>b.onclick=()=>{const blob=new Blob([`<h1>LUNA Preview</h1>`],{type:'text/html'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='luna-preview.html';a.click();URL.revokeObjectURL(a.href);toast('ダウンロードを開始しました')});$$('[data-preview]',root).forEach(b=>b.onclick=()=>openPreview());$$('[data-regenerate]',root).forEach(b=>b.onclick=()=>{toast('回答を再生成しています…');setTimeout(()=>toast('新しい回答を生成しました'),700)})}
function openPreview(){const w=window.open('','_blank','width=700,height=600');if(w)w.document.write('<!doctype html><title>LUNA Preview</title><style>body{font:16px system-ui;padding:40px}h1{font-size:32px}</style><h1>LUNA Preview</h1><p>生成HTMLのプレビューです。</p>')}
$('#webBtn').onclick=()=>{$('#webBtn').classList.toggle('selected');toast($('#webBtn').classList.contains('selected')?'Web検索を有効化':'Web検索を無効化')};
$('#settingsOpen').onclick=()=>$('#settingsBackdrop').classList.add('show');$('#settingsClose').onclick=()=>$('#settingsBackdrop').classList.remove('show');$('#settingsBackdrop').onclick=e=>{if(e.target===e.currentTarget)e.currentTarget.classList.remove('show')};
$$('.settings-tab').forEach(t=>t.onclick=()=>{$$('.settings-tab').forEach(x=>x.classList.remove('active'));$$('.settings-page').forEach(x=>x.classList.remove('active'));t.classList.add('active');$(`.settings-page[data-page="${t.dataset.tab}"]`).classList.add('active')});
$$('.swatch').forEach(s=>s.onclick=()=>{$$('.swatch').forEach(x=>x.classList.remove('selected'));s.classList.add('selected')});
$$('.segmented button').forEach(b=>b.onclick=()=>{$$('.segmented button').forEach(x=>x.classList.remove('selected'));b.classList.add('selected')});
$('#demoToggle').onclick=()=>{renderShowcase();$('#showcaseBackdrop').classList.add('show')};$('#showcaseClose').onclick=()=>$('#showcaseBackdrop').classList.remove('show');$('#showcaseBackdrop').onclick=e=>{if(e.target===e.currentTarget)e.currentTarget.classList.remove('show')};
function renderShowcase(){const b=$('#showcaseBody');b.innerHTML=`<div class="showcase-grid"><section><h3>モデル</h3><div class="demo-panel">${models.map(m=>`<div class="demo-model"><span class="dot"></span><div><b>${esc(m.name)}</b><small>${esc(m.desc)}</small></div></div>`).join('')}</div></section><section><h3>回答表示</h3><div class="demo-panel">${demoAnswer()}</div></section><section><h3>ファイル状態</h3><div class="demo-panel file-states"><div class="state"><b>アップロード</b><span>PDF / 画像 / コード</span></div><div class="state loading"><b>読み込み中</b><span>ファイルを解析しています…</span></div><div class="state error"><b>読み込み失敗</b><span>ファイルを読み込めませんでした</span></div><div class="state success"><b>完了</b><span>内容を読み取りました</span></div></div></section><section><h3>入力・検索</h3><div class="demo-panel"><div class="fake-input">LUNAにメッセージを入力…<button>↑</button></div><div class="source-box"><div><b>Web Sources</b><small>検索結果カードの表示</small></div><button>開く ›</button></div></div></section><section><h3>空・ローディング・エラー</h3><div class="demo-panel states"><div class="empty-state"><b>まだ会話はありません</b><small>新しいチャットを始めましょう。</small></div><div class="thinking"><span></span><span></span><span></span><b>考えています…</b></div><div class="error-state"><b>回答を取得できませんでした</b><button>再試行</button></div></div></section><section><h3>生成ファイル</h3><div class="demo-panel"><div class="preview-card"><div><span class="preview-icon">SVG</span><div><b>diagram.svg</b><small>SVG · 8.1 KB</small></div></div><div class="preview-actions"><button>プレビュー</button><button>↓ ダウンロード</button></div></div><div class="preview-card"><div><span class="preview-icon">JSON</span><div><b>result.json</b><small>JSON · 1.2 KB</small></div></div><div class="preview-actions"><button>開く</button><button>↓ ダウンロード</button></div></div></div></section></div>`}
$('#moreBtn').onclick=()=>$('#settingsOpen').click();
document.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();$('#newChat').click()}if(e.key==='Escape'){ $$('.modal-backdrop').forEach(x=>x.classList.remove('show'));$('#context').classList.remove('show');closeFile()}});
